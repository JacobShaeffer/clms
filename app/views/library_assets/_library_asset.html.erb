<div id="<%= dom_id library_asset %>">
  <h2 class="card-title h5"><%= library_asset.name %></h2>

  <p class="card-text mb-0">
    <strong>Language:</strong>
    <%= library_asset.language.presence || "Not specified" %>
  </p>
</div>
