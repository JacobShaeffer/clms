<%= render layout: "shared/modal", locals: {
  title: content.persisted? ? "Edit content" : "New content",
  dialog_class: ("modal-nearly-fullscreen" unless content.persisted?)
} do %>
  <%= render "contents/form", content: content %>
<% end %>
