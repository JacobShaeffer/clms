<%= render layout: "shared/modal", locals: {
  title: content.persisted? ? "Edit content" : "New content",
  dialog_class: "modal-nearly-fullscreen"
} do %>
  <%= render "contents/form", content: content, modal: true %>
<% end %>
