<button
  type="button"
  class="list-group-item list-group-item-action<%= " active" if selected %>"
  id="<%= component_id %>-selector-<%= metadatum.id %>"
  data-metadatum-id="<%= metadatum.id %>"
  data-content-multi-select-target="selector"
  data-action="mousedown->content-multi-select#onItemSelected"
>
  <%= metadatum.name %>
</button>
